# Dependencies for the live Streamlit dashboard (streamlit run dashboard.py).
# Install on top of requirements.txt:
#     pip install -r requirements-dashboard.txt
streamlit>=1.20,<2.0
streamlit-autorefresh>=1.0
pandas>=1.3,<3.0
